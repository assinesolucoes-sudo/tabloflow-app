# Spec — Bloco V-0: Validação dos Motores # Arquivo: src/teste_motores.py # Sessão Claude Code: UMA sessão, fechar ao concluir --- ## OBJETIVO Validar que motor_upload.py e motor_base.py produzem contratos corretos (UploadResult e MotorResult) antes de qualquer visão ser testada. Nenhuma visão deve ser chamada neste bloco. Nenhum arquivo real de disco deve ser necessário — tudo em memória. --- ## ARQUIVOS ENVOLVIDOS - src/motor_upload.py → lê, valida e normaliza arquivos → retorna UploadResult - src/motor_base.py → recebe UploadResult → infere tipos, nulos, flags → retorna MotorResult - src/teste_motores.py → este arquivo (a criar) --- ## ESTRATÉGIA DE TESTE Usar io.BytesIO para simular arquivos Excel em memória com openpyxl. Não usar pytest. Usar funções simples com assert + print de resultado. Ao final: imprimir resumo PASSOU/FALHOU por caso. Estrutura do script: def caso_XX_nome() -> bool: # monta base sintética # chama motor_upload → verifica UploadResult # chama motor_base → verifica MotorResult # retorna True se passou, False se falhou if __name__ == "__main__": casos = [caso_01_..., caso_02_..., ...] resultados = [(c.__name__, c()) for c in casos] for nome, ok in resultados: print(f"{'✅' if ok else '❌'} {nome}") total = sum(1 for _, ok in resultados if ok) print(f"\n{total}/{len(resultados)} casos passaram") --- ## CASOS DE TESTE OBRIGATÓRIOS ### GRUPO A — Motor de Upload (UploadResult) **A01 — Excel simples, 1 aba, colunas mistas** - Base: 20 linhas, colunas: id (int), nome (str), valor (float), data (date), ativo (bool) - Esperado: success=True, n_abas=1, colunas detectadas corretamente, sem errors **A02 — Excel com múltiplas abas** - Base: 2 abas ("Vendas" e "Metas"), cada uma com 10 linhas - Esperado: success=True, n_abas=2, lista de abas correta **A03 — Arquivo com linha de cabeçalho vazia (coluna sem nome)** - Base: 1 coluna sem nome no header - Esperado: warning disparado sobre coluna sem nome OU renomeação automática **A04 — Arquivo com encoding problemático (simular via nome de coluna com acento)** - Base: coluna chamada "Descrição" com valores normais - Esperado: success=True, coluna preservada corretamente **A05 — Arquivo vazio (0 linhas de dados, só header)** - Esperado: success=False OU warning crítico sobre base vazia **A06 — Arquivo com mais de 100.000 linhas (simular com 100.001)** - Esperado: warning de volume alto OU error de limite, conforme contrato --- ### GRUPO B — Motor Base (MotorResult) **B01 — Inferência de tipo NUMERIC** - Coluna com valores: [1.0, 2.5, 3.0, None, 4.0] - Esperado: tipo="numeric", null_count=1, null_pct=0.2 **B02 — Inferência de tipo TEXT** - Coluna com valores: ["SP", "RJ", "MG", "SP", None] - Esperado: tipo="text", is_candidate_categorical=True (cardinalidade baixa) **B03 — Inferência de tipo DATE** - Coluna com valores: [date(2024,1,1), date(2024,2,1), date(2024,3,1)] - Esperado: tipo="date" **B04 — Inferência de tipo BOOLEAN** - Coluna com valores: [True, False, True, True, False] - Esperado: tipo="boolean" **B05 — Inferência de tipo MIXED** - Coluna com valores: [1, "texto", 2.5, None, True] - Esperado: tipo="mixed", warning disparado **B06 — Coluna com null-strings** - Coluna com valores: ["N/A", "n/a", "-", "", " ", "NULL", "none", "valor"] - Esperado: null_count >= 6 (as 6 null-strings tratadas como nulo) - Esperado: o valor "valor" NÃO é nulo **B07 — Candidato categórico por cardinalidade** - Coluna TEXT com 3 valores únicos em 50 registros - Esperado: is_candidate_categorical=True **B08 — Não-candidato categórico por cardinalidade alta** - Coluna TEXT com 60 valores únicos em 60 registros - Esperado: is_candidate_categorical=False (CATEGORICAL_THRESHOLD=50 conforme implementação) **B09 — Seleção de aba: arquivo com 1 aba** - Esperado: aba selecionada automaticamente, sem error **B10 — Seleção de aba: arquivo com múltiplas abas, aba_selecionada=None** - Esperado: warning de ambiguidade OU seleção da primeira aba com log **B11 — Coluna completamente vazia** - Coluna com todos os valores None ou null-string - Esperado: tipo="empty", null_pct=1.0 **B12 — MotorResult completo: verificar campos obrigatórios** - Qualquer base válida - Verificar que MotorResult contém: - colunas: List[ColumnMeta] - n_linhas: int > 0 - n_colunas: int > 0 - warnings: List[str] - errors: List[str] - success: bool - aba_processada: str --- ### GRUPO C — Integração Upload → Base **C01 — Pipeline completo: Excel simples → UploadResult → MotorResult** - Montar Excel em memória → motor_upload → motor_base - Verificar que MotorResult.n_linhas == linhas reais da base - Verificar que MotorResult.n_colunas == colunas reais **C02 — Pipeline com coluna de data como string ISO** - Coluna "data" com valores "2024-01-01", "2024-02-01" (strings, não date) - Esperado: motor_base infere tipo="date" OU tipo="text" com warning (documentar qual comportamento o motor implementou) **C03 — Pipeline com base mista: numérico + texto + data + bool + nulos** - Base com 5 colunas de tipos diferentes, 30 linhas, ~10% nulos por coluna - Verificar que cada coluna tem tipo correto no MotorResult - Verificar null_count correto por coluna --- ## CONTRATOS ESPERADOS (referência) ### UploadResult (campos mínimos a verificar) ```python result.success: bool result.errors: List[str] result.warnings: List[str] result.abas: List[str] # nomes das abas detectadas result.n_abas: int result.arquivo_bytes: bytes # ou equivalente para passar ao motor_base
MotorResult (campos mínimos a verificar)

result.success: bool result.errors: List[str] result.warnings: List[str] result.n_linhas: int result.n_colunas: int result.aba_processada: str result.colunas: List[ColumnMeta] # Por coluna (ColumnMeta): col.nome: str col.tipo: str # "numeric"|"text"|"date"|"boolean"|"mixed"|"empty" col.null_count: int col.null_pct: float col.is_candidate_categorical: bool col.n_unique: int
REGRAS DA SESSÃO

Leia CONTEXT.md antes de começar
Leia esta spec inteira antes de escrever qualquer linha
Implemente APENAS src/teste_motores.py
Não modifique motor_upload.py nem motor_base.py
Se um contrato não existir como esperado, documente no print do caso
(não quebre o script — capture a exceção e marque como FALHOU)
Ao final da sessão: rodar o script e reportar o resultado completo
ASSINATURA DA FUNÇÃO PRINCIPAL

# Não há função de entrada — o script é executado diretamente: # python src/teste_motores.py # Cada caso retorna True (passou) ou False (falhou) # O script imprime o resultado de cada caso e o total ao final
PENDÊNCIAS A DOCUMENTAR

Se qualquer contrato divergir do esperado (campo com nome diferente,
tipo diferente, comportamento diferente), o Claude Code deve:

Marcar o caso como FALHOU
Imprimir a divergência encontrada vs. esperada
NÃO corrigir o motor — apenas documentar
As divergências serão tratadas em sessão separada (Bloco V-0b se necessário).
